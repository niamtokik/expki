defmodule Expki.PublicKey.RSAPrivateKey do
  @moduledoc """
  
  ```elixir
  alias Expki.PublicKey.RSAPrivateKey

  true = RSAPrivateKey.is_record?({'RSAPrivateKey', ...})
  ```
  """

  use Expki.PublicKey, name: :"RSAPrivateKey"

end
